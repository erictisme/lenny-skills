# Round 4: lenny-learn Skill + Repo Cleanup

3 tasks. Create the `/lenny-learn` skill (works with ANY article), update install script and README.

---

- [x] **Task 1: Create `/lenny-learn` skill** — Create `skills/lenny-learn.md`. This is a DIFFERENT kind of skill from the existing 18 theme-based journeys. Instead of a fixed curriculum of 5-8 concepts across multiple articles, `/lenny-learn` works with ANY SINGLE article from Lenny's 638-piece archive. The user provides an article title or filename as an argument (e.g., `/lenny-learn pricing-your-saas-product`). The skill then: (1) Uses `mcp__lennysdata__read_content` to load that specific article, (2) Runs a Tal Raviv-style tutoring flow on that ONE article: overview of the article → extract 3-5 key concepts → teach each concept one at a time → check understanding → apply to the user's situation → wrap up with action items. **Format reference:** Look at `skills/lenny-pricing.md` for the coaching session structure (opening, per-concept flow, closing, rules). Also reference `~/.claude/commands/study.md` for the Socratic tutoring pattern (how to ask questions, hold a high bar, check understanding). **Key differences from theme skills:** (1) No hardcoded curriculum — concepts are extracted dynamically from whatever article is loaded, (2) No hardcoded source articles list — works with any filename, (3) The opening question asks what the user is working on so the session can be personalized, (4) Shorter session — 3-5 concepts from one article vs 5-8 from many. **The skill file must follow the security constraints in CLAUDE.md** (no bash commands, no silent file writes, no network calls beyond MCP). Keep it under 100 lines — it should be readable in 2 minutes.

- [x] **Task 2: Update install.sh** — The install script at `install.sh` already copies all `.md` files from `skills/` to `~/.claude/commands/`. Verify that the script uses a glob pattern (`"$SKILLS_DIR"/*.md`) so it automatically picks up `lenny-learn.md`. If it does, update the count and description text: change "18 coaching journey skills" to "19 skills" (18 coaching journeys + 1 per-article learning tool). If it doesn't use a glob, add `lenny-learn.md` explicitly.

- [x] **Task 3: Update README.md** — Add `/lenny-learn` to the skills table in `README.md`. It should appear at the TOP of the table (before the 18 theme skills) since it's the most flexible skill. Row: `| /lenny-learn | Deep-dive into any single article with guided tutoring | Anyone exploring Lenny's archive |`. Also update the header count from "18 coaching journey skills" to "19 skills (18 coaching journeys + 1 per-article deep learning tool)" or similar. In the "How It Works" section, add a brief note that `/lenny-learn` works differently — it takes any article as input rather than following a fixed curriculum.
